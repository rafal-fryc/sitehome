---
phase: 12-patterns-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pattern-merge-config.json
  - public/data/ftc-patterns.json
autonomous: true
requirements:
  - PATN-01
  - PATN-03

must_haves:
  truths:
    - "Remedy pattern categories are consolidated per the 7 user-directed merge groups from CONTEXT.md"
    - "Structural (boilerplate) patterns are flagged for later UI separation"
    - "ftc-patterns.json contains ~35 patterns after consolidation (down from 52)"
  artifacts:
    - path: "scripts/pattern-merge-config.json"
      provides: "7 new merge groups added to existing config"
      contains: "data-protection-program"
    - path: "public/data/ftc-patterns.json"
      provides: "Rebuilt pattern data with consolidated categories"
  key_links:
    - from: "scripts/pattern-merge-config.json"
      to: "scripts/build-patterns.ts"
      via: "Pass 3 merge config loading"
      pattern: "MERGE_CONFIG_PATH"
    - from: "scripts/build-patterns.ts"
      to: "public/data/ftc-patterns.json"
      via: "writeJSONSafe output"
      pattern: "OUT_FILE"
---

<objective>
Apply the 7 user-directed remedy pattern merge groups to consolidate overlapping categories, then rebuild the patterns data file.

Purpose: Reduce the 52 existing remedy patterns to ~35 by merging related categories per user decisions made during phase discussion. This is the data foundation for PATN-01 (remedy patterns as distinct category) and PATN-03 (category consolidation).

Output: Updated `pattern-merge-config.json` with 7 new merge groups and rebuilt `ftc-patterns.json` reflecting consolidated categories.
</objective>

<execution_context>
@C:/Users/rafst/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rafst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-patterns-overhaul/12-CONTEXT.md

<interfaces>
<!-- Existing merge config structure in scripts/pattern-merge-config.json -->
<!-- The file already has 15 merge_groups and 4 prune_list entries -->
<!-- Each merge_group: { target_name, target_id, topic_area, source_patterns[], source_names[], estimated_unique_cases, variant_count, year_range, rationale } -->

<!-- build-patterns.ts reads MERGE_CONFIG_PATH and applies Pass 3 merge logic -->
<!-- It loads merge_groups, combines variants from source patterns into merged targets -->
<!-- Then removes source patterns + pruned patterns, adds merged groups -->

From src/types/ftc.ts:
```typescript
export interface PatternGroup {
  id: string;
  name: string;
  is_structural: boolean;
  case_count: number;
  variant_count: number;
  year_range: [number, number];
  most_recent_year: number;
  most_recent_date?: string;
  enforcement_topics: string[];
  practice_areas: string[];
  variants: PatternVariant[];
}

export interface PatternsFile {
  generated_at: string;
  total_patterns: number;
  total_variants: number;
  patterns: PatternGroup[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 new merge groups to pattern-merge-config.json</name>
  <files>scripts/pattern-merge-config.json</files>
  <action>
Add 7 new merge groups to the existing `merge_groups` array in `scripts/pattern-merge-config.json`. The file already has 15 merge groups — append these 7 new ones. Use the same structure as existing entries.

**The 7 merge groups (from CONTEXT.md decisions):**

1. **Annual Certification** (target_id: `annual-certification`)
   - Source patterns: `annual-certification`, `annual-certifications`
   - Topic area: structural
   - Estimated ~35 cases

2. **Notice and Affirmative Express Consent** (target_id: `notice-and-affirmative-express-consent`)
   - Source patterns: `notice-and-affirmative-express-consent-provision`, `notice-and-affirmative-express-consent`
   - Topic area: consent
   - Estimated ~6 cases

3. **Data Protection Program** (target_id: `data-protection-program`)
   - Source patterns: `privacy-program`, `mandated-privacy-and-information-security-program`, `information-security-program`, `protection-of-data`
   - Topic area: security
   - Estimated ~95 cases
   - NOTE: Check if `privacy-program` and `information-security-program` already exist as merge targets in the existing config. If so, those existing merge groups already consolidate sub-patterns into those IDs. The new merge group should reference the *target IDs* from existing merges (not the original source pattern IDs that were already consumed). Specifically: the existing config already merges several patterns into `privacy-program` and `information-security-program`. This new merge group should list those target IDs as sources, effectively merging the already-merged groups into the new `data-protection-program` super-group.

4. **Tracking & Surveillance Restrictions** (target_id: `tracking-surveillance-restrictions`)
   - Source patterns: `monitoring-technology-prohibited`, `use-of-tracking-technology-limited`, `sensitive-location-data-program`, `location-data-deletion-requests`
   - Topic area: surveillance
   - Estimated ~26 cases

5. **Biometric Data Protections** (target_id: `biometric-data-protections`)
   - Source patterns: `use-of-facial-recognition-or-analysis-systems-prohibited`, `deletion-of-covered-biometric-information`, `mandated-automated-biometric-security-or-surveillance-system-monitoring-program`, `mandatory-notice-and-complaint-procedures-for-automated-biometric-security-or-surveillance-systems`, `required-retention-limits-for-biometric-information`, `disclosure-of-automated-biometric-security-or-surveillance-systems`
   - Topic area: biometric
   - Estimated ~3-5 cases

6. **Data Lifecycle Requirements** (target_id: `data-lifecycle-requirements`)
   - Source patterns: `data-deletion-requirements`, `data-retention-limits`
   - Topic area: data-handling
   - NOTE: `data-deletion-requirements` is already a merge target in the existing config. This new merge should reference that target ID.

7. **Financial Remedies** (target_id: `financial-remedies`)
   - Source patterns: `civil-penalty`, `monetary-relief-and-judgment`, `costs-and-fees`
   - Topic area: monetary
   - Estimated ~138 cases
   - NOTE: All three are already merge targets in the existing config. This super-merge combines them.

**Important:** Before adding, run `npx tsx scripts/build-patterns.ts` once to see the current pattern IDs so you can verify the source_patterns IDs match real pattern IDs in the output. Grep the current ftc-patterns.json for the expected source IDs. If a source pattern doesn't exist (because it was never a standalone pattern or was already consumed by an earlier merge), adjust accordingly — only include IDs that actually appear as pattern IDs in the current output.

For each new entry, include `source_names` (human-readable original names) alongside `source_patterns` (slugified IDs) per the existing format.
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('scripts/pattern-merge-config.json','utf8')); console.log('merge_groups:', c.merge_groups.length); const newIds=['annual-certification','notice-and-affirmative-express-consent','data-protection-program','tracking-surveillance-restrictions','biometric-data-protections','data-lifecycle-requirements','financial-remedies']; const found=newIds.filter(id=>c.merge_groups.some(function(g){return g.target_id===id})); console.log('New groups found:', found.length, '/', newIds.length); if(found.length<7) process.exit(1)"</automated>
  </verify>
  <done>pattern-merge-config.json contains all 7 new merge groups with correct target_ids, source_patterns referencing valid existing pattern IDs, and descriptive metadata</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild ftc-patterns.json with consolidated categories</name>
  <files>public/data/ftc-patterns.json</files>
  <action>
Run `npx tsx scripts/build-patterns.ts` to rebuild the patterns file with the updated merge config.

After rebuilding, verify:
1. Total pattern count is lower than the previous 52 (expect ~35 patterns)
2. The 7 new merge target IDs appear in the output
3. No source pattern IDs from the merges remain as standalone patterns
4. The JSON is valid and the PatternsFile shape is preserved

If the build script errors on any of the new merge groups (e.g., source pattern ID not found), fix the source_patterns in the merge config to reference the correct IDs and re-run.

Print a before/after comparison showing the pattern count change.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('public/data/ftc-patterns.json','utf8')); console.log('Patterns:', d.total_patterns); console.log('Has financial-remedies:', d.patterns.some(function(p){return p.id==='financial-remedies'})); console.log('Has data-protection-program:', d.patterns.some(function(p){return p.id==='data-protection-program'})); if(d.total_patterns >= 52) process.exit(1)"</automated>
  </verify>
  <done>ftc-patterns.json has been rebuilt with ~35 patterns reflecting all 7 new merge groups, no orphaned source patterns remain, and the file validates as PatternsFile shape</done>
</task>

</tasks>

<verification>
1. `scripts/pattern-merge-config.json` has 22 merge groups (15 original + 7 new)
2. `public/data/ftc-patterns.json` loads with fewer than 52 patterns
3. All 7 new target IDs exist in the patterns output
4. App still builds: `npx next build` (or at minimum `npx tsc --noEmit`)
</verification>

<success_criteria>
- Remedy patterns consolidated from ~52 to ~35 per user-directed merge decisions
- All 7 merge groups from CONTEXT.md decisions are applied
- ftc-patterns.json validates and is served correctly
- Existing build-patterns.ts pipeline handles the new merges without code changes (config-only)
</success_criteria>

<output>
After completion, create `.planning/phases/12-patterns-overhaul/12-01-SUMMARY.md`
</output>
