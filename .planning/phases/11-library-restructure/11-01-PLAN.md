---
phase: 11-library-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ftc/FTCProvisionsTab.tsx
  - src/components/ftc/provisions/TopicSidebar.tsx
  - src/components/ftc/FTCTabShell.tsx
autonomous: true
requirements: [LIB-01, LIB-02]
must_haves:
  truths:
    - "Provisions library tab has two sub-tabs: 'By Topic' and 'By Case'"
    - "Default sub-tab when entering provisions is 'By Case'"
    - "Practice Area section does not appear in the sidebar"
    - "Sidebar only appears on the 'By Topic' sub-tab"
    - "Selecting 'By Topic' with no topic selected shows a brief instruction instead of ProvisionsLanding"
    - "Practice area URLs (e.g., ?topic=pa-deception) still load and display data correctly"
  artifacts:
    - path: "src/components/ftc/FTCProvisionsTab.tsx"
      provides: "Sub-tab layout with 'By Topic' and 'By Case' switching"
      contains: "TabsTrigger.*By Topic|TabsTrigger.*By Case"
    - path: "src/components/ftc/provisions/TopicSidebar.tsx"
      provides: "Sidebar with only Statutory Authority and Remedy Type categories"
  key_links:
    - from: "src/components/ftc/FTCProvisionsTab.tsx"
      to: "src/components/ftc/provisions/TopicSidebar.tsx"
      via: "TopicSidebar rendered only when sub-tab is 'By Topic'"
      pattern: "subtab.*topic.*TopicSidebar"
---

<objective>
Restructure the provisions library tab into a two-section layout with sub-tabs and remove practice area navigation from the sidebar.

Purpose: Separates provision browsing into topic-based and case-based workflows, making the library more navigable for different user intents.
Output: FTCProvisionsTab with sub-tab routing, TopicSidebar without practice area section.
</objective>

<execution_context>
@C:/Users/rafst/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rafst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-library-restructure/11-CONTEXT.md

@src/components/ftc/FTCProvisionsTab.tsx
@src/components/ftc/provisions/TopicSidebar.tsx
@src/components/ftc/provisions/ProvisionsLanding.tsx
@src/components/ftc/provisions/ProvisionsContent.tsx
@src/components/ftc/FTCTabShell.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ftc/FTCProvisionsTab.tsx (current structure to restructure):
- Uses useSearchParams for topic, q, scope URL params
- Renders TopicSidebar + ProvisionsContent/ProvisionsLanding/SearchResults
- handleTopicSelect, handleSearchChange, handleSearchScopeChange callbacks

From src/components/ftc/provisions/TopicSidebar.tsx:
```typescript
interface Props {
  manifest: ProvisionsManifest;
  selectedTopic: string | null;
  onSelectTopic: (topicSlug: string) => void;
}

const CATEGORY_ORDER: { key: ManifestTopic["category"]; heading: string }[] = [
  { key: "statutory", heading: "Statutory Authority" },
  { key: "practice_area", heading: "Practice Area" },
  { key: "remedy_type", heading: "Remedy Type" },
];
```

From src/components/ui/tabs.tsx:
```typescript
export { Tabs, TabsList, TabsTrigger, TabsContent }
// Radix UI Tabs primitive — value/onValueChange pattern
```

From src/hooks/use-provisions.ts:
```typescript
export function useProvisionsManifest(): UseQueryResult<ProvisionsManifest>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove practice area from TopicSidebar</name>
  <files>src/components/ftc/provisions/TopicSidebar.tsx</files>
  <action>
  Remove the practice_area entry from the CATEGORY_ORDER array and the grouped record initialization. The sidebar should only show Statutory Authority and Remedy Type sections.

  Specific changes in TopicSidebar.tsx:
  1. Remove `{ key: "practice_area", heading: "Practice Area" }` from CATEGORY_ORDER array
  2. Remove `practice_area: []` from the groups Record initialization in the useMemo
  3. The for-of loop over manifest.topics entries will still push practice_area topics into groups, but since there is no `practice_area` key in groups anymore, the `groups[topic.category]?.push(...)` optional chain will safely skip them
  4. Do NOT remove or modify any data fetching — practice_area topics still exist in the manifest and can still be loaded via direct URL (e.g., ?topic=pa-deception)
  5. Both desktop nav and mobile nav sections iterate CATEGORY_ORDER, so removing from CATEGORY_ORDER removes from both

  Per user decision: "Practice area URLs still load data if accessed directly — just no sidebar links." The sidebar simply stops rendering navigation links for practice areas. ProvisionsContent will still load the shard data when a practice_area topic slug appears in the URL.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TopicSidebar renders only Statutory Authority and Remedy Type sections. Practice area topics are not visible in the sidebar but remain loadable via direct URL access.</done>
</task>

<task type="auto">
  <name>Task 2: Add sub-tab layout to FTCProvisionsTab</name>
  <files>src/components/ftc/FTCProvisionsTab.tsx, src/components/ftc/FTCTabShell.tsx</files>
  <action>
  Restructure FTCProvisionsTab.tsx to use Radix/shadcn Tabs component for sub-tab navigation between "By Topic" and "By Case" views.

  **FTCProvisionsTab.tsx — full restructure:**

  1. Add import for Tabs, TabsList, TabsTrigger, TabsContent from @/components/ui/tabs
  2. Add import for useFTCData from @/hooks/use-ftc-data (needed for case data in "By Case" tab — Plan 02 will consume this)
  3. Remove the ProvisionsLanding import entirely — per user decision, "Remove ProvisionsLanding page entirely"

  4. Add sub-tab state using URL search params:
     - Read `view` param from searchParams: `const viewParam = searchParams.get("view") as "topic" | "case" | null`
     - Default to "case" when no view param present (per user decision: "Default sub-tab is 'By Case'")
     - Create handleViewChange callback that sets/clears the `view` param. When switching to "case", clear topic/q/scope params. When switching to "topic", clear case-specific params.

  5. Restructure the return JSX:
     - Wrap the content in a Tabs component with value={activeView} onValueChange={handleViewChange}
     - TabsList with two TabsTrigger elements: "By Topic" (value="topic") and "By Case" (value="case")
     - Style TabsList to sit below the main tab header — use `className="mb-4"` or similar spacing
     - TabsContent value="topic": render the existing sidebar + ProvisionsContent layout (the `<div className="flex gap-6 py-6">` block with TopicSidebar and content area)
     - TabsContent value="case": render a placeholder div with text "Case browser loading..." (Plan 02 will replace this with the CaseBrowser component)

  6. For the "By Topic" tab content when no topic is selected (previously showed ProvisionsLanding):
     - Instead of rendering ProvisionsLanding, show a brief instruction inline: a centered div with text like "Select a topic from the sidebar to browse provisions by statutory authority or remedy type." Use the existing design tokens: font-garamond, text-muted-foreground, py-16, text-center.
     - This replaces the old ProvisionsLanding entirely.

  7. Keep all existing search/filter/scope handlers for the "By Topic" sub-tab — they only apply when viewing topic-based provisions.

  **FTCTabShell.tsx — minor update:**
  In the handleTabChange callback, when switching TO the provisions tab, preserve the `view` param if it exists. Currently handleTabChange creates a fresh URLSearchParams and only sets the tab. Update it so when value is "provisions", it also carries the `view` param. Actually, this is not needed since the default "By Case" view is already the default without a param. Leave FTCTabShell unchanged unless the sub-tab default behavior needs it.

  Actually, on reflection, FTCTabShell.tsx does NOT need changes. The handleTabChange clears all params, and "By Case" is the default when no `view` param is present. This is correct behavior.

  **Important:** Do NOT delete ProvisionsLanding.tsx file yet — it may be imported elsewhere. Just remove the import and usage from FTCProvisionsTab.tsx. The file can be cleaned up later.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>FTCProvisionsTab renders two sub-tabs ("By Topic" and "By Case"). Default view is "By Case". "By Topic" shows sidebar + provisions content without practice area. "By Case" shows placeholder for Plan 02. ProvisionsLanding is no longer used.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with zero errors
2. Vite production build succeeds
3. Navigate to ?tab=provisions — lands on "By Case" sub-tab (default)
4. Click "By Topic" sub-tab — sidebar appears with only Statutory Authority and Remedy Type
5. Select a topic — provisions load normally with existing filters and search
6. Navigate directly to ?tab=provisions&topic=pa-deception — provisions load (backward compat)
7. Practice Area heading does not appear in sidebar in either desktop or mobile view
</verification>

<success_criteria>
- Sub-tab navigation renders correctly with "By Topic" and "By Case" options
- "By Case" is the default landing view
- Practice Area category is absent from the sidebar
- All existing topic-based provision browsing continues to work
- Direct practice area URLs still load provision data
- TypeScript and build pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-library-restructure/11-01-SUMMARY.md`
</output>
