# External Integrations

**Analysis Date:** 2026-02-26

## APIs & External Services

**AI / Machine Learning:**
- Anthropic Claude API - Used in data pipeline scripts to classify FTC legal provisions with statutory topics, practice areas, remedy types, and industry sectors
  - SDK/Client: `@anthropic-ai/sdk` ^0.78.0 (devDependency, not shipped to browser)
  - Model: `claude-sonnet-4-5` (hardcoded in `scripts/classify-provisions.ts`)
  - Auth: `ANTHROPIC_API_KEY` environment variable
  - Usage: One-time/batch classification script only — `scripts/classify-provisions.ts`

**Content / RSS:**
- Substack RSS Feed - Articles synced from `https://rafalfryc.substack.com/feed`
  - Consumed by: Supabase Edge Function `supabase/functions/sync-substack/index.ts`
  - Parser: `fast-xml-parser` 4.2.5 (loaded via esm.sh in Deno runtime)
  - Sync trigger: Manual via "Refresh" button in `src/components/ArticlesSection.tsx`
  - No authentication required (public RSS feed)

**UI Platform:**
- Lovable.dev - Visual development platform integration
  - Plugin: `lovable-tagger` 1.1.9 (devDependency, active only in `mode === 'development'`)
  - Configured in: `vite.config.ts` via `componentTagger()` plugin

## Data Storage

**Databases:**
- Supabase (PostgreSQL) - Managed Postgres instance
  - Project ID: `tltdghkyunvhayvxtudq`
  - URL: `https://tltdghkyunvhayvxtudq.supabase.co` (hardcoded in `src/integrations/supabase/client.ts`)
  - Client: `@supabase/supabase-js` 2.55 — typed via auto-generated `src/integrations/supabase/types.ts`
  - Auth mode: Anon key (public read), service role key used only in edge functions
  - Tables:
    - `public.articles` — stores Substack articles synced via RSS (id, title, excerpt, content, substack_url, published_date, read_time, category, created_at, updated_at)
  - Row Level Security: Enabled on `articles` table; public SELECT policy, no public INSERT/UPDATE
  - Trigger: `update_articles_updated_at` auto-sets `updated_at` on row modification

**Static JSON (file-based data store):**
- FTC case data served as static files from `public/data/`
  - `public/data/ftc-cases.json` — aggregated case summaries with groupings and analysis
  - `public/data/ftc-files/*.json` — individual classified case files
  - Generated by `scripts/build-ftc-data.ts`, `scripts/build-provisions.ts`, `scripts/build-patterns.ts`
  - Fetched at runtime via `fetch()` + TanStack Query in `src/hooks/use-ftc-data.ts`, `src/hooks/use-provisions.ts`, `src/hooks/use-patterns.ts`

**File Storage:**
- Local filesystem only (data pipeline reads from `C:/Users/rafst/Documents/projectas/FTC/output_v2`)
- No cloud file storage (no S3/R2/GCS integration)

**Caching:**
- TanStack Query in-memory cache for static JSON data (no explicit staleTime configuration observed)

## Authentication & Identity

**Auth Provider:**
- Supabase Auth (configured but minimal use)
  - Implementation: `localStorage` session persistence, auto token refresh
  - Config: `src/integrations/supabase/client.ts`
  - Current usage: Articles table uses public anon key for read access; no user login flows detected in frontend routes
  - Edge functions: Service role key used server-side for write operations

## Monitoring & Observability

**Error Tracking:**
- None detected — no Sentry, Datadog, or similar SDK

**Logs:**
- `console.log` / `console.error` throughout — no structured logging framework
- Supabase Edge Function logs via Supabase dashboard (standard Deno console output)

## CI/CD & Deployment

**Hosting:**
- Vercel — SPA deployment
  - Config: `vercel.json`
  - SPA fallback: All routes except `/data`, `/GDPRorders`, `/checklists` rewrite to `/index.html`
  - Static HTML: `/GDPRorders` served from `public/GDPRorders.html`

**External Route Proxying:**
- `/checklists` and `/checklists/:path*` proxied to `https://law-list-buddy-34.vercel.app` — a separate Vercel application
- `/checklists/api/:path*` also proxied to the same external app

**CI Pipeline:**
- None detected (no GitHub Actions, CircleCI, or similar config found)

**Edge Functions:**
- Supabase Edge Functions (Deno runtime)
  - `supabase/functions/sync-substack/index.ts` — RSS sync function
  - JWT verification disabled for `sync-substack` (per `supabase/config.toml`)

## Webhooks & Callbacks

**Incoming:**
- None detected (no webhook receiver endpoints in frontend or documented edge functions)

**Outgoing:**
- Supabase Edge Function makes outbound HTTP request to `https://rafalfryc.substack.com/feed` (RSS fetch)

## Environment Configuration

**Required env vars (development/scripts):**
- `ANTHROPIC_API_KEY` — required for `scripts/classify-provisions.ts` AI classification pipeline

**Required env vars (Supabase edge functions — set in Supabase dashboard):**
- `SUPABASE_URL` — injected automatically by Supabase runtime
- `SUPABASE_SERVICE_ROLE_KEY` — injected automatically by Supabase runtime

**Frontend (hardcoded public values in `src/integrations/supabase/client.ts`):**
- `SUPABASE_URL` = `https://tltdghkyunvhayvxtudq.supabase.co`
- `SUPABASE_PUBLISHABLE_KEY` = JWT anon key (safe for public use, RLS enforced)

**Secrets location:**
- `.env` file at project root (contents not read; likely contains `ANTHROPIC_API_KEY`)
- Supabase service role key stored in Supabase dashboard secrets, not in codebase

---

*Integration audit: 2026-02-26*
